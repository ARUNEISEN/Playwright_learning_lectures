# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: Assertions.spec.js >> Demonstrating assertions in Playwright
- Location: tests\Assertions.spec.js:3:5

# Error details

```
Error: expect(locator).not.toBeEmpty() failed

Locator:  locator('#currentAddress')
Expected: not empty
Received: empty
Timeout:  5000ms

Call log:
  - Expect "not toBeEmpty" with timeout 5000ms
  - waiting for locator('#currentAddress')
    13 × locator resolved to <textarea rows="5" cols="20" id="currentAddress" class="form-control" placeholder="Current Address"></textarea>
       - unexpected value "empty"

```

```yaml
- textbox "Current Address"
```

# Test source

```ts
  1  | import {test,epect,locator, expect} from '@playwright/test'
  2  | 
  3  | test('Demonstrating assertions in Playwright', async ({page}) => {
  4  |     await page.goto('https://demoqa.com/text-box');
  5  |     console.log('Title of the page', await page.title());
  6  |     console.log('URL of the page', await page.url());
  7  |     await expect(page).toHaveTitle('demosite');
  8  |     await expect(page).toHaveURL('https://demoqa.com/text-box');
  9  | 
  10 |     const nameTextbox = page.getByPlaceholder('Full Name');
  11 |     await expect(nameTextbox).toBeVisible();
  12 |     await expect(nameTextbox).toBeEnabled();
  13 |     await expect(nameTextbox).toBeEditable();
  14 |     await expect(nameTextbox).not.toBeHidden();
  15 |     await expect(nameTextbox).not.toBeDisabled();
  16 | 
  17 |     await nameTextbox.fill('Arunkumar');
  18 |     await expect(nameTextbox).toHaveValue('Arunkumar');
  19 | 
  20 |     await expect(page.locator('#currentAddress-label')).toHaveText('Current Address');
  21 | 
> 22 |     await expect(page.locator('#currentAddress')).not.toBeEmpty();
     |                                                       ^ Error: expect(locator).not.toBeEmpty() failed
  23 | 
  24 |     await expect(page.locator('#permanentAddress')).toBeEmpty();
  25 | 
  26 |     const submitBtn = page.locator('#submit');
  27 |     await expect(submitBtn).toBeEnabled();
  28 | 
  29 |     await page.waitForTimeout(4000);
  30 |     
  31 | })
```